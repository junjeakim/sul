# 애플리케이션 이름 및 포트 설정
spring.application.name=backend
server.port=8081

# 데이터베이스 연결 설정
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3306/zzan?serverTimezone=UTC&characterEncoding=UTF-8
spring.datasource.username=root
spring.datasource.password=1234

# 실행 쿼리 콘솔 출력 설정
spring.jpa.properties.hibernate.show_sql=true
spring.jpa.properties.hibernate.format_sql=true
logging.level.org.hibernate.type.descriptor.sql=trace

# Hibernate JPA 관련 설정
spring.jpa.hibernate.ddl-auto=none
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl

# Spring의 로그 레벨 설정
logging.level.org.springframework=DEBUG
logging.level.root=DEBUG


# JWT Secret Key 설정
jwt.secret-key=yourSecretKey

# 로그 파일 설정 (선택 사항)
#logging.file.name=logs/application.log

# JPA의 패키지 이름 (필요시, 엔티티 스캔)
#spring.jpa.properties.hibernate.packagesToScan=com.back
